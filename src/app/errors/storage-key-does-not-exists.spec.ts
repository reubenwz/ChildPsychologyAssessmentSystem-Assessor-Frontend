import { StorageKeyDoesNotExists } from './storage-key-does-not-exists';

describe('StorageKeyDoesNotExists', () => {
  it('should create an instance', () => {
    expect(new StorageKeyDoesNotExists()).toBeTruthy();
  });
});
